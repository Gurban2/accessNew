import React from 'react';
// import './AddPer.scss';  // You can create this SCSS file to add styles later

const AddPer = () => {
  return <h1 className="user-permissions-add">User & Permissions - Add Permission</h1>;
};

export default AddPer;